const WarehouseTableStyle = theme => ({
  root: {
    marginTop: theme.spacing.unit * 2,
    overflowX: "auto"
  },
  table: {
    minWidth: 700,
    width: "100%"
  }
});

export default WarehouseTableStyle;
