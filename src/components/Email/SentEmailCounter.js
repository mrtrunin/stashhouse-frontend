import React from "react";
import PropTypes from "prop-types";

const SentEmailCounter = ({ sentEmails }) => {
  return <div />;
};

SentEmailCounter.propTypes = {};

export default SentEmailCounter;
